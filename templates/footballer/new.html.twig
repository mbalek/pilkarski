{% extends 'base_admin.html.twig' %}

{% block title %}
    {{ 'footballer.display.newFootballer'|trans }}
{% endblock %}

{% block body %}

        <div class="card-header">
            <h2>{{ 'footballer.display.newFootballer'|trans }}</h2>
        </div>
        
        <div class="card-body">{{ include('footballer/_form.html.twig') }}</div>
        
        <div class="card-footer">
            <a href="{{ path('club_show', {'id': club.id , 'league': league.id}) }}">
                <button class="btn btn-primary btn-block">{{ 'global.actions.backToList'|trans }}</button>
            </a>
        </div>


    {% endblock %}
