{% extends 'base_admin.html.twig' %}

{% block title %}{{ 'footballer.display.newFootballer'|trans }}{% endblock %}

{% block body %}
    <div class="data-table-area">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                    <div class="data-table-list">
                        <div class="basic-tb-hd">
                            <h2>{{ 'footballer.display.newFootballer'|trans }}</h2>
                        </div>
                        {{ include('footballer/_form.html.twig') }}
                        <p>
                            <a href="{{ path('club_show', {'id': club.id , 'league': league.id}) }}"><button class="btn btn-primary btn-block">{{ 'global.actions.backToList'|trans }}</button></a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
