<div class="dashboard-header">
    <nav class="navbar navbar-expand-sm bg-white fixed-top">
        <a href="{{ path('admin_panel') }}" class="navbar-brand text-primary">Pilkarski</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse " id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto navbar-right-top">
                <li class="nav-item dropdown nav-user">

                    <a class="nav-link nav-user-img" href="#" id="navbarDropdownMenuLink2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <span>Zalogowano jako: </span> <span class="text-secondary">{{ app.user.username }}</span>
                        {#<img src="assets/images/avatar-1.jpg" alt="" class="user-avatar-md rounded-circle">#}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right nav-user-dropdown" aria-labelledby="navbarDropdownMenuLink2">
                        <div class="nav-user-info">
                            <h5 class="mb-0 text-white nav-user-name">{{ app.user.username }}</h5>
                        </div>
                        <!-- w tym linku będą mecze nadchodzące które może komentować dany mod -->
                        <a class="dropdown-item" href="#"><i class="fas fa-cog mr-2"></i>{{ 'topbar.user.myMatches' |trans }}</a>
                        <a class="dropdown-item" href="./logout"><i class="fas fa-user mr-2"></i>{{ 'global.actions.logout' |trans }}</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>
</div>
