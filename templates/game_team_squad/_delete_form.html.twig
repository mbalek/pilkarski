<form method="post" action="{{ path('game_team_squad_delete', {'id': game_team_squad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game_team_squad.id) }}">
    <button class="btn">Delete</button>
</form>
