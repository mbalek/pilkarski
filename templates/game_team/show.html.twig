{% extends 'base.html.twig' %}

{% block title %}GameTeam{% endblock %}

{% block body %}
    <h1>GameTeam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_team.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_team_index') }}">back to list</a>

    <a href="{{ path('game_team_edit', {'id': game_team.id}) }}">edit</a>

    {{ include('game_team/_delete_form.html.twig') }}
{% endblock %}
