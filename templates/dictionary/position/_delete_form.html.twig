<form method="post" action="{{ path('dictionary_position_delete', {'id': position.id}) }}" onsubmit="return confirm('{{ 'global.actions.deleteMessage'|trans }}');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ position.id) }}">
    <button class="dropdown-item text-danger">{{ 'global.delete' |trans }}</button>
</form>
