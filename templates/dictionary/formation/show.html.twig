{% extends 'base_admin.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ formation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dictionary_formation_index') }}">back to list</a>

    <a href="{{ path('dictionary_formation_edit', {'id': formation.id}) }}">edit</a>

    {{ include('dictionary_formation/_delete_form.html.twig') }}
{% endblock %}
