<form method="post" action="{{ path('round_delete', {'id': round.id , 'league': league}) }}" onsubmit="return confirm('{{ 'global.actions.deleteMessage'|trans }}');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ round.id) }}">
    <button class="btn">{{ 'global.delete' |trans }}</button>
</form>
