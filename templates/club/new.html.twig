{% extends 'base_admin.html.twig' %}

{% block title %}{{ 'club.display.newClub'|trans }}{% endblock %}

{% block body %}
<div class="card-header">
    <h2>{{ 'club.display.newClub'|trans }}</h2>
</div>
<div class="card-body">
{{ include('club/_form.html.twig') }}
</div>
<div class="card-footer">
    <a href="{{ path('league_show' , {'id':league.id}) }}">
        <button class="btn btn-primary btn-block">{{ 'global.actions.backToList'|trans }}</button>
    </a>
</div>
{% endblock %}
