{% extends 'base.html.twig' %}

{% block title %}{{ 'global.action.adminDash' |trans }}{% endblock %}

{% block body %}

    <div class="data-table-area">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                    <div class="data-table-list">
                        <div class="basic-tb-hd">
                            <h2>{{ 'task.display.taskToDo' }}</h2>
                        </div>
                        <div class="table-responsive">
                            <table id="data-table-basic" class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>{{ 'task.fields.title' |trans }}</th>
                                        <th>{{ 'task.fields.finishDate' |trans }}</th>
                                        <th>{{ 'task.fields.assigmentDate' |trans }}</th>
                                        <th>{{ 'task.fields.description' |trans }}</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- tutaj foreach po pozycjach z tabeli -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
<!--
<div class="example-wrapper">
    <a href="{{ path('club_index') }}">club</a>
    <a href="{{ path('events_index') }}">events</a>
    <a href="{{ path('footballer_index') }}">footballer</a>
    <a href="{{ path('league_index') }}">league</a>
    <a href="{{ path('game_index') }}">match</a>
    <a href="{{ path('game_team_index') }}">match team</a>
    <a href="{{ path('round_index') }}">round</a>
    <a href="{{ path('game_team_squad_index') }}">match team squad</a>
    <a href="{{ path('user_index') }}">user</a>
</div>
-->
{% endblock %}
